import React from "react";
import {useHistory} from "react-router-dom";

function blog() {
    let history = useHistory();

    history.push()

    return(
        <div>
            
        </div>
    )
}